name: dotweave
description: >-
  Cross-platform dotfiles sync CLI. Dart port of @tinyrack/dotweave.
version: 2.4.0
publish_to: none
environment:
  sdk: ^3.12.0

executables:
  dotweave: dotweave

dependencies:
  cliweave: ^0.2.2
  cryptography: ^2.7.0
  dartage: ^0.2.0
  ffi: ^2.1.0
  path: ^1.9.0
  win32: ^6.3.0

dev_dependencies:
  lints: ^6.0.0
  shipworld: ^0.2.0
  test: ^1.26.0
