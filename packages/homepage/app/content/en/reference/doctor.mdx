---
title: "dotweave doctor"
description: "Check the health of your sync setup."
section: reference
order: 8
---

Check sync directory, config, age identity, and tracked local paths.

Run health checks for the local sync setup, including repository availability, config validity, age identity configuration, and whether tracked local paths still exist where dotweave expects them.

## Usage

```bash
dotweave doctor [flags]
```

## Flags

| Flag               | Description                          |
| ------------------ | ------------------------------------ |
| `--profile <name>` | Use a specific profile layer.        |

## Examples

Run all health checks:

```bash
dotweave doctor
```

Run checks for a specific profile:

```bash
dotweave doctor --profile work
```
