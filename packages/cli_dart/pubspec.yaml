name: dotweave
description: >-
  Cross-platform dotfiles sync CLI. Dart port of @tinyrack/dotweave.
version: 0.53.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

executables:
  dotweave: dotweave

dependencies:
  cryptography: ^2.7.0
  ffi: ^2.1.0
  path: ^1.9.0
  win32: ^5.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
