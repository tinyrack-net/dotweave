tags:
  # Cross-implementation tests against the npm age-encryption package (needs node).
  interop:
  # TS↔Dart cross-CLI compatibility harness (needs the built TS CLI).
  compat:
  # PTY-driven shell tests (POSIX only).
  pty:
