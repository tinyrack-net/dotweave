{
  "name": "@tinyrack/dotweave",
  "version": "0.51.0",
  "description": "A personal CLI tool for git-backed configuration sync.",
  "type": "module",
  "license": "MIT",
  "author": "winetree94",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tinyrack-net/dotweave.git"
  },
  "homepage": "https://github.com/tinyrack-net/dotweave#readme",
  "bugs": {
    "url": "https://github.com/tinyrack-net/dotweave/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "cli",
    "sync",
    "dotfiles",
    "config-sync",
    "dotweave"
  ],
  "imports": {
    "#app/*.ts": {
      "@dotweave/source": "./src/*.ts",
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "bin": {
    "dotweave": "./bin/index.js"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "tsc -p tsconfig.build.json --watch",
    "build": "tsc -p tsconfig.build.json",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "prebuild": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "pkg:build": "dotweave-tools pkg build",
    "pkg:smoke": "dotweave-tools pkg smoke",
    "start": "node dist/index.js",
    "prepack": "pnpm run build",
    "pretest": "pnpm run build",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:autocomplete": "vitest run src/services/autocomplete.test.ts tests/autocomplete.e2e.test.ts tests/autocomplete.pty.e2e.test.ts",
    "test:watch": "vitest",
    "precoverage": "pnpm run build",
    "coverage": "vitest run --coverage",
    "check": "pnpm run typecheck && biome check . && pnpm run test",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "release:patch": "dotweave-tools release patch",
    "release:minor": "dotweave-tools release minor",
    "release:major": "dotweave-tools release major"
  },
  "dependencies": {
    "@stricli/core": "^1.2.8",
    "age-encryption": "^0.3.0",
    "picocolors": "^1.1.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@tinyrack/dotweave-tools": "workspace:*",
    "@types/node": "^24.13.2",
    "@vitest/coverage-v8": "^4.1.9",
    "execa": "^9.6.1",
    "node-gyp": "^13.0.0",
    "node-pty": "^1.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
