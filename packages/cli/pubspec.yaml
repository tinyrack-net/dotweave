name: dotweave
description: >-
  Cross-platform dotfiles sync CLI. Dart port of @tinyrack/dotweave.
version: 2.0.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

executables:
  dotweave: dotweave

dependencies:
  cryptography: ^2.7.0
  dotweave_age: ^0.1.0
  ffi: ^2.1.0
  path: ^1.9.0
  tinyrack_cli: ^0.1.0
  win32: ^5.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
